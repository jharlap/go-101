// Declare a struct type to contain information about a person.  Include a name and age.  Declare a function that pretty-prints values of your type.  Create a slice of people.  In `main` call your pretty-printer for each person.
package main

// (add your imports here.)

// Person is someone with a name and age
// (declare your type here...)

// Pretty generates a string containing pretty-print output for a Person
// (declare your func here...)

func main() {
	// (declare and populate a slice of Person)

	// (loop over the slice, generating the pretty-print strings and outputting each)
}
